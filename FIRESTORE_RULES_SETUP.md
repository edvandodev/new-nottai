Passos r?pidos para aplicar as regras do Firestore:
1. Abra Firebase Console > Firestore Database > Rules.
2. Substitua as regras atuais pelo conte?do de `firestore.rules`.
3. Clique em Publish para salvar.

Observa??o: os dados do app devem ser salvos em `users/<uid>/...`. Cole??es globais antigas ficam inacess?veis por padr?o.
